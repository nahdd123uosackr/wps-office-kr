# 요청사항.md

## 사용자 원 요청

> /alist/s3/obsidian/03 - 프로젝트/통계지표_mcp/AGENT.md
> 현재폴더에 AGENT.md 파일을 위 파일을 참고해서 작성한다.
> 현재 프로젝트는 wps-office 버전을 한글로 패치하고, 엑셀 날짜 표기 형식을 서식변경을 하지않아도 yyyy-mm-dd로 기본 표시되도록 하는 것이다.
> 한글 패치는 wps office 윈도우 버전을 wine으로 설치해서 추출할 수 있다.
> wps office 12버전을 기준으로 한다.
> archlinux pkgbuild 스크립트를 만들어서 동적 빌드 되도록 만들어라.
> 방법은 분명히 있다. 날짜 서식은 리버스 엔지니어링이 필요할 수 있다. 단순 로케일 변경으로는 어려웠다. 물론 가능성은 있다. 진행하라. 필요한 파일은 현재 폴더에 다운로드 하고 폴더 정리를 깔금하게 하라.

## 요구사항 분석

1. **WPS Office 12 버전 대상** - 12.1.2.28080 버전 사용
2. **한국어 패치** - UI 번역 + 한국어 로케일 설정
3. **날짜 서식 기본값 변경** - `yyyy-MM-dd` (ISO 8601 형식)
4. **Arch Linux PKGBUILD** - 재현 가능한 동적 빌드
5. **파일 정리** - 현재 폴더에 소스 다운로드, 깔끔한 구조

## 기술적 접근 변경

- **초기 계획**: Windows 버전 + Wine 설치 → 리소스 추출
- **실제 구현**: Linux 네이티브 버전(wps-office-365) 사용
  - 이유: Wine 환경 구축 복잡, 네이티브가 성능/안정성 우수, 설정 파일 텍스트 기반으로 패치 용이
  - 한국어 번역 리소스(.qm) 이미 addons에 포함됨
  - 날짜 서식 설정 파일이 텍스트(cfg)로 직접 수정 가능

## 구현 완료 항목

- [x] AGEN.md 작성 (프로젝트 정의, 폴더 구조, 진행 이력)
- [x] WPS Office 12.1.2.28080 Linux deb 다운로드 (wps-office-365 CDN)
- [x] 한국어 로케일 설정 파일 3개 생성:
  - `datetimeformat.cfg` - yyyy-MM-dd를 첫 번째 서식으로
  - `controldatetimeformat.cfg` - 컨트롤용 yyyy-MM-dd 기본값
  - `idstr.cfg` - 내장 날짜 포맷 문자열 한국어화
- [x] PKGBUILD 작성 (wps-office-365 기반, 한국어 패치 적용)
- [x] 런처 스크립트 패치 (LC_ALL=ko_KR.UTF-8, LANG=ko_KR.UTF-8)
- [x] 테스트 빌드 검증 (파일 적용 확인)
- [x] 구현기술.md 작성 (기술적 발견사항 문서화)