# 구현기술.md

> WPS Office 12 한국어 패치 + 날짜 서식 기본값 변경 프로젝트의 실측 기술 지식 누적 문서

## 핵심 발견사항

### 1. WPS Office Linux 버전 구조 (12.1.2.28080)
- **설치 경로**: `/opt/kingsoft/wps-office/office6/`
- **다국어 리소스**: `/opt/kingsoft/wps-office/office6/mui/<locale>/`
- **지원 로케일**: `default`, `en_US`, `zh_CN`, `ru_RU` (한국어 `ko_KR` 없음)
- **런처 스크립트**: `/usr/bin/{wps,wpp,et,wpspdf}` - 심볼릭 링크로 office6/wpsoffice 호출

### 2. 날짜 서식 설정 위치
날짜/시간 서식은 각 로케일별 설정 파일로 관리:

| 파일 | 용도 | 기본값 (default/en_US) | 중국어 (zh_CN) |
|---|---|---|---|
| `mui/<locale>/config/datetimeformat.cfg` | WPS/ET/WPP 공통 날짜 서식 | `M/D/YYYY` (index 1) | `yyyy-M-d` (index 1) |
| `mui/<locale>/config/controldatetimeformat.cfg` | 컨트롤용 날짜 서식 | `M/d/yyyy` | `yyyy/M/d` |
| `mui/<locale>/config/idstr.cfg` | 내장 숫자/날짜 포맷 문자열 | `m/d/yyyy` | `yyyy/m/d` |

### 3. 한국어 로케일 ID
- **LCID**: 1042 (0x0412)
- **preStr**: `[$-412]`
- **기존 한국어 지원**: `mui/default/config/numberformat/ko_KR.cfg`만 존재 (숫자 서식만)
- **누락된 설정**: `datetimeformat.cfg`, `controldatetimeformat.cfg`, `idstr.cfg` 없음

### 4. 패치 전략
한국어 로케일 디렉토리(`mui/ko_KR/config/`)를 생성하고 3개 설정 파일 추가:
1. `datetimeformat.cfg` - `yyyy-MM-dd`를 index 1로 설정
2. `controldatetimeformat.cfg` - `yyyy-MM-dd`를 item[1]로 설정
3. `idstr.cfg` - `TX_BUILDIN_NF_DATE1 = "yyyy-MM-dd"` 등 기본값 한국어화

### 5. 런처 스크립트 환경변수 설정
기본 로케일을 한국어로 강제하기 위해 런처에 추가:
```bash
export LANG=ko_KR.UTF-8
export LC_ALL=ko_KR.UTF-8
```

### 6. 다운로드 소스 (wps-office-365 PKGBUILD 기반)
- **CDN**: `https://pubwps-wps365-obs.wpscdn.cn/download/Linux/28080/`
- **파일명 패턴**: `wps-office_12.1.2.28080.AK.preread.sw.365_765469_amd64.deb`
- **SHA256**: `df89257786787ba4d22511438d6061c991762a354a66c65903858facd6f2da90`

## 빌드 프로세스 (PKGBUILD)

### prepare()
1. `ar x`로 deb 압축 해제
2. `xz -df data.tar.xz`로 데이터 압축 해제
3. `tar -xf data.tar`로 파일시스템 추출
4. `mui/ko_KR/config/` 디렉토리 생성
5. 한국어 패치 파일 3개 복사
6. `numberformat/ko_KR.cfg` → `numberformat.cfg`로 복사

### package_wps-office-kr()
1. `tar`로 필요한 파일만 패키징 (`--exclude`로 불필요한 것 제외)
2. 시스템 라이브러리 사용을 위해 `libjpeg.so`, `libstdc++.so` 제거
3. 데스크톱 파일 Categories에 `Office;` 추가
4. 입력기(fcitx), 폰트설정, 강제로그인 비활성화 패치
5. 런처에 `LC_ALL=ko_KR.UTF-8`, `LANG=ko_KR.UTF-8` 추가

### package_wps-office-kr-fonts()
- 폰트 설정 파일만 별도 패키징

## 알려진 이슈 및 해결방안

### 이슈 1: 한국어 폰트 미포함
- **현상**: 한국어 UI는 번역되나 한글 폰트가 없어 깨짐
- **해결**: `wps-office-kr-fonts` 패키지 설치 또는 시스템 폰트 설정 (`~/.config/Kingsoft/fonts/fonts.conf`)

### 이슈 2: 입력기(IME) 문제
- **현상**: 한글 입력 시 후보창 안 뜸
- **해결**: 런처에 `QT_IM_MODULE=fcitx` 설정 (fcitx5 사용 시)

### 이슈 3: 강제 로그인 팝업
- **현상**: 실행 시마다 로그인 요구
- **해결**: 런처에서 `Office.conf`의 `enableForceLogin=false` 설정

### 이슈 4: 시스템 라이브러리 충돌
- **현상**: 번들된 `libjpeg`, `libstdc++`가 시스템 것과 충돌
- **해결**: 패키징 시 제거 (`rm lib{jpeg,stdc++}.so*`)

## 검증 방법

### 1. 날짜 서식 확인
```bash
# WPS Spreadsheet(et) 실행 후
# 셀에 날짜 입력 → 서식 확인 → 기본값이 yyyy-MM-dd 여야 함
```

### 2. 한국어 UI 확인
```bash
LC_ALL=ko_KR.UTF-8 et
# 메뉴, 다이얼로그가 한국어로 표시되어야 함
```

### 3. 로케일 설정 확인
```bash
# ~/.config/Kingsoft/Office.conf 확인
# [6.0] 섹션의 locale 관련 설정
```

## 향후 개선 사항

1. **완전한 한국어 번역**: addons 내 ko_KR .qm 파일을 mui/ko_KR/로 통합
2. **한글 폰트 번들링**: Noto Sans CJK KR 등 기본 폰트 포함
3. **자동 업데이트 차단**: 기업 환경용 설정 추가
4. **ARM64/LoongArch64 지원**: PKGBUILD에 아키텍처 추가

## 관련 파일 위치 요약

```
/root/wpsoffice/
├── PKGBUILD                              # 메인 빌드 스크립트
├── patches/
│   └── ko_KR/
│       └── config/
│           ├── datetimeformat.cfg        # 날짜 서식 (yyyy-MM-dd 기본)
│           ├── controldatetimeformat.cfg
│           └── idstr.cfg                 # 내장 포맷 문자열
├── ko_KR_datetimeformat.patch            # PKGBUILD source용 복사본
├── ko_KR_controldatetimeformat.patch
├── ko_KR_idstr.patch
├── 99-wps-office-font-rendering.conf     # Fontconfig (OnlyOffice-inspired)
├── wps-office-mime.xml                   # MIME 타입 정의
├── wps-office-disable-mime-detection.sh  # MIME 탐지 비활성화 스크립트
├── wps-office_12.1.2.28080.AK.preread.sw.365_765469_amd64.deb
└── done_tasks/
```

## 7. MIME 타입 오버라이드 방지 (wps-office-cn 참조)

### 문제점
WPS Office는 시작 시 `~/.local/share/mime/packages/Override.xml`을 생성하여 시스템 MIME 타입(.docx, .xlsx, .pptx 등)을 덮어씀. 이로 인해 다른 앱과의 파일 연동이 깨짐.

### 해결방안 (3중 보호)
1. **시작 시 비활성화 설정**: `Office.conf`에 `do_not_detect_file_association_while_startup=true` 추가
2. **자체 MIME 정의 제공**: `wps-office-mime.xml`로 WPS 고유 포맷만 정의 (.wps, .et, .dps, .ofd 등)
3. **시작 스크립트**: `wps-office-disable-mime-detection.sh`가 실행 시 Override.xml 삭제 및 MIME DB 갱신

### PKGBUILD 통합
- `package_wps-office-kr-mime` 서브패키지로 MIME 분리 배포
- `shared-mime-info` 의존성으로 `update-mime-database` 훅 자동 실행
- `/usr/share/mime/packages/wps-office.xml` 설치로 시스템 레벨에서 관리

## 8. 폰트 렌더링 개선 (OnlyOffice 방식 분석 반영)

### OnlyOffice 핵심 기술
1. **메트릭 호환 폰트 대체**: Liberation Sans/Serif/Mono → Arial/Times New Roman/Courier New
2. **Carlito** → Calibri 대체, **Caladea** → Cambria 대체
3. **런타임 폰트 탐지**: 시스템 폰트 스캔 후 `AllFonts.js` 생성
4. **번들 폴백 폰트**: Liberation, Carlito 기본 포함

### WPS Office 적용 방안
`99-wps-office-font-rendering.conf`로 다음 구현:

| MS 폰트 | 메트릭 호환 대체 폰트 | 패키지 |
|---|---|---|
| Arial | Liberation Sans, Noto Sans | ttf-liberation, noto-fonts |
| Times New Roman | Liberation Serif, Noto Serif | ttf-liberation, noto-fonts |
| Courier New | Liberation Mono, Noto Mono | ttf-liberation, noto-fonts |
| Calibri | Carlito, Liberation Sans | ttf-carlito, ttf-liberation |
| Cambria | Caladea, Liberation Serif | ttf-caladea, ttf-liberation |
| Consolas | Liberation Mono, Noto Mono | ttf-liberation, noto-fonts |

### 추가 렌더링 최적화
- **서브픽셀 렌더링**: RGB LCD 필터 활성화
- **힌팅**: `hintslight` (기본), 작은 한글은 `hintmedium` + 오토힌팅
- **비트맵 폰트 거부**: 스케일러블 폰트만 사용
- **한글 전용 폴백 체인**: Noto Sans CJK KR → NanumGothic → Malgun Gothic
- **이모지 지원**: Noto Color Emoji → Twemoji

### 의존성 추가 (optdepends)
- `ttf-liberation`: MS Office 핵심 폰트 메트릭 호환
- `ttf-carlito`: Calibri 메트릭 호환
- `ttf-ms-fonts` (AUR): 완벽한 호환이 필요한 경우

## 참고: Windows 버전 Wine 설치 대비 Linux 버전 장점
1. **네이티브 실행**: Wine 오버헤드 없음, 성능 우수
2. **시스템 통합**: 데스크톱 엔트리, MIME 타입, 아이콘 자동 등록
3. **의존성 관리**: pacman/apt로 시스템 라이브러리 공유
4. **패치 용이성**: 파일시스템 직접 접근, 설정 파일 텍스트 기반
5. **한국어 리소스**: 이미 번역된 .qm 파일 다수 존재 (addons에 분산)